APP_ENV=development
# 로컬 개발 전용 값이다. 배포에서는 프로비저너가 postgresql+psycopg:// URL을 주입한다.
# 이 파일을 .env로 복사해야 앱이 뜬다 - 값이 없으면 조용히 SQLite로 넘어가지 않고
# 즉시 실패한다(사내 배포 규약 R4-4).
DATABASE_URL=sqlite:///./data/prototype.db
APP_BASE_URL=http://127.0.0.1:8501
